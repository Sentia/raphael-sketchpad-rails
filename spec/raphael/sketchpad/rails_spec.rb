require "spec_helper"

describe Raphael::Sketchpad::Rails do
  it "has a version number" do
    expect(Raphael::Sketchpad::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
